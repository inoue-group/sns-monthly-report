# SNS月次レポート管理アプリ セットアップ手順

`index.html`（と`app.js`）は 2 つのモードで動きます。

| モード | 状態 | 保存先 |
|---|---|---|
| ローカル demo モード | Firebase 未設定（初期状態） | 開いた端末のブラウザのみ |
| クラウド同期モード | Firebase 設定を貼り付け済み | 全拠点で共有（リアルタイム） |

まず demo モードで画面と使い勝手を確認し、問題なければ下記の手順でクラウド同期にして公開します。
**完全無料（クレジットカード登録・課金なし）** で運用できます。

---

## STEP 1. Firebase プロジェクトを作る（無料）

1. Google アカウントで <https://console.firebase.google.com/> を開く
2. 「プロジェクトを追加」→ プロジェクト名（例：`sns-report`）→ 作成
   - Google アナリティクスは「無効」で OK
3. 左メニュー **構築 → Firestore Database** →「データベースの作成」
   - ロケーション：`asia-northeast1`（東京）
   - **「テストモードで開始」** を選択して作成

## STEP 2. ウェブアプリを登録して設定をコピー

1. プロジェクトの概要（歯車アイコン）→ **プロジェクトの設定**
2. 「マイアプリ」→ **ウェブ（`</>`）** アイコンをクリック
3. アプリのニックネーム（例：`web`）を入力 →「アプリを登録」
4. 表示される `const firebaseConfig = { ... }` の中身をコピー

## STEP 3. index.html に貼り付け

`index.html` を右クリック →「プログラムから開く」→ メモ帳。
ファイル上部の次の部分を、STEP 2 でコピーした値に置き換えて保存します。

```js
window.SNS_FIREBASE_CONFIG = {
  apiKey: "コピーした値",
  authDomain: "コピーした値",
  projectId: "コピーした値",
  storageBucket: "コピーした値",
  messagingSenderId: "コピーした値",
  appId: "コピーした値"
};
```

保存してブラウザで開き直すと、右上の表示が **「● クラウド同期中」** に変われば成功です。
（この時点で初期データ＝彩house・F.PRODUCTS・INOUE-GROUPの3ブランドと用語一覧が自動で作成されます）

## STEP 4. Firestore のルールを設定（誰でも利用可にする）

テストモードのルールは **約 30 日で自動的に使えなくなる** ため、公開ルールに置き換えます。

Firebase コンソール → **Firestore Database → ルール** タブ → 下記に全置換して「公開」：

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

> ⚠️ これは「URL を知っている人は誰でも読み書きできる」設定です（ご要望どおり）。

## STEP 5. Web に公開する（無料）

`index.html` / `app.js` / `assets` フォルダ一式をまとめて公開します。

### GitHub Pages（推奨・パンフレット発注管理アプリと同じ方式）
1. GitHub に新しいリポジトリを作成し、この `SNS月次レポート管理` フォルダの中身一式（`index.html`, `app.js`, `assets/`）を push する
2. リポジトリの Settings → Pages → Source を「Deploy from a branch」、Branch を `main` / `(root)` に設定
3. 数分後、`https://<ユーザー名>.github.io/<リポジトリ名>/` で公開される

### その他（Netlify Drop / Cloudflare Pages）
フォルダごとドラッグ＆ドロップするだけでも公開できます。

---

## 使い方の要点

- **閲覧**：ブランド → SNS を選ぶと、その月の実績・グラフが表示されます（初期状態は閲覧モード）
- **入力**：右上の「編集する」ボタンで入力モードに切り替え、数値を入力して保存
- **今月の投稿・リンククリック数**：画像・数値を登録するとカードで一覧表示されます（Instagramのみ）
- **PDFで保存**：レポート画面下の「PDFで保存」ボタンから、ブラウザの印刷機能でPDF保存できます
- **用語一覧**：トップページ下部の「用語一覧」から、SNS専門用語の検索・追加ができます

## こんなときは

| 症状 | 対処 |
|---|---|
| 右上が「ローカル demo モード」のまま | STEP 3 の貼り付け内容・保存を確認 |
| 「クラウドに接続できません」と赤帯 | STEP 4 のルールが未設定／古い可能性 |
| データを移したい／消したい | Firebase コンソールの Firestore Database 画面から直接編集可能 |
